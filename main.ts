//utiliser le fetch
//https://developer.mozilla.org/fr/docs/Web/API/Fetch_API/Using_Fetch

/*fetch("quiz.json")
    .then(function(response) { return response.json(); })
    .then(function(donne) {
        
      //document.querySelector('.home-box custom-box').innerHTML = "Le thème est : " + donne.theme;
      console.log("le nombre de qt : " + donne.nombreQts);
      console.log("le theme est : " + donne.theme);
    });

//const questionNumber = document.querySelector(".home-box custom-box");
//const questionText = document.querySelector(".question-text");
//const optionContainer = document.querySelector(".question-container");



*/









